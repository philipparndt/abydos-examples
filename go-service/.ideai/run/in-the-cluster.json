{
  "name": "in the cluster",
  "type": "go",
  "request": "launch",
  "program": "${workspaceFolder}",
  "cwd": "${workspaceFolder}",
  "env": { "STAGE": "dev" },
  "ideai.devPod": {
    "context": "${currentContext}",
    "allowedContexts": "k3c-*, k3d-*, *-local",
    "namespace": "ideai-examples",
    "port": 8080
  }
}
