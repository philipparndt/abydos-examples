{
  "name": "in the cluster",
  "type": "go",
  "request": "launch",
  "program": "${workspaceFolder}",
  "cwd": "${workspaceFolder}",
  "env": { "STAGE": "dev" },
  "abydos.devPod": {
    "context": "${currentContext}",
    "allowedContexts": "k3c-*, k3d-*, *-local",
    "namespace": "abydos-examples",
    "port": 8080
  }
}
