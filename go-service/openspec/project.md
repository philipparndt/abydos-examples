# go-service

Invented, like the backlog beside it in `.abydos/backlog`. Three changes, so
that the OpenSpec cards have a part-finished one, a nearly-finished one and
one with no `tasks.md` at all — which is meant to show no fraction rather
than `0/0`.

The `openspec` CLI is not needed to read any of it: a change is committed
markdown, and the pane reads the directory.
