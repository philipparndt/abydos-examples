# The control. No rule in `.sops.yaml` matches this name and there is nothing
# in it worth hiding, so there is no chip and no lock: a file the editor says
# nothing about is most files.
listen: :8080
log_level: info
retries: 3
