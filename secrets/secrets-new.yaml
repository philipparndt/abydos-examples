# Nothing in this file is encrypted, and `.sops.yaml` matches its name — which
# is the pair of facts the status bar is looking for. The chip offers to
# encrypt it; doing so writes the ciphertext over this file, and the values
# below are gone from the working copy.
#
# `git checkout secrets/secrets-new.yaml` puts it back.
mqtt:
  password: correct-horse-battery
