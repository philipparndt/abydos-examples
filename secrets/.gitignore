# The two files make-fixtures.sh writes that git is meant to ignore. The third
# one it writes — .env.staging — is deliberately absent from this list: a file
# git neither tracks nor ignores is the whole of what the status bar's
# *Not in .gitignore* is about, and a line here would take the fixture away.
.env.local
*.dec
