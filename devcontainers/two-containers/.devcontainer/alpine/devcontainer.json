{
  // One of two. The third legal place for a devcontainer.json is
  // .devcontainer/<anything>/devcontainer.json, which is how a project offers a
  // choice of containers to work in.
  //
  // Nothing is wrong with this file. It is the plainest one in the repository.
  // The `name` below is what the menu entry offering it reads; without one, the
  // folder — "alpine" — would be, which is what keeps two entries apart.
  "name": "Small",
  "image": "alpine:3.21"
}
