# openscad

One file, no setup. Open `bracket.scad`, change `depth` or `fillet`, and the
preview follows — the model is re-rendered from the file on disk, so saving is
the only step.

Worth trying: the structure pane lists the modules; go-to-definition on
`plate(...)` jumps to it.
